apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "portal.fullname" . }}-mfe-hello
  labels:
    {{- include "portal.labels" . | nindent 4 }}
    app.kubernetes.io/component: mfe-hello
spec:
  replicas: {{ .Values.mfeHello.replicaCount }}
  selector:
    matchLabels:
      {{- include "portal.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: mfe-hello
  template:
    metadata:
      labels:
        {{- include "portal.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: mfe-hello
    spec:
      containers:
        - name: mfe-hello
          image: "{{ .Values.mfeHello.image.repository }}:{{ .Values.mfeHello.image.tag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: nginx-conf
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: mfe.conf
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            {{- toYaml .Values.mfeHello.resources | nindent 12 }}
      volumes:
        - name: nginx-conf
          configMap:
            name: {{ include "portal.fullname" . }}-nginx
